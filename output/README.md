# Local runtime artifacts

This directory stores the accepted Stage-0 dataset and runtime outputs. Git
ignores every file here except this note. Restore large artifacts from the
verified sibling archive when needed; do not commit Parquet datasets, model
weights, predictions, or logs.
